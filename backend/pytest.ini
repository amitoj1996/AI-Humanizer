[pytest]
testpaths = tests
asyncio_mode = auto
filterwarnings =
    ignore::DeprecationWarning
markers =
    eval: slow regression-gate tests that load real models (run via `pytest -m eval`)
# Default run excludes eval — keeps the fast test suite fast.
addopts = -m "not eval"
